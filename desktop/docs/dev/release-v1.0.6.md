## What's Changed

- Fixed Grok Build `responses` compatibility when upstream falls back through `chat/completions` by preserving required `output_text.annotations` fields.
- Added key-management context action to activate selected keys at the end of the advanced-proxy queue, with toggle-to-remove behavior.
- Added Dispatch candidate filtering for unqueued provider cards and tightened the Dispatch toolbar layout.
- Kept the five advanced-proxy app takeover buttons on one row after adding Grok Build.

**Full Changelog**: https://github.com/jlwebs/AllApiDeck/compare/v1.0.5...v1.0.6
